name: Sdel Database Keep Alive
on:
  schedule:
    - cron: '0 0 */2 * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Execute Warmer
        # This line MUST match your file name exactly
        run: node supabase-keep-alive.js
        
